# import random

# def get_random_values(input_list):
#     # Check if the input list has at least 3 elements
#     if len(input_list) < 3:
#         return "Input list must have at least 3 elements"
    
#     # Use random.sample to get 3 random values from the list
#     random_values = random.sample(input_list, 3)
#     return random_values

# # Example usage:
# my_list = ['apple', 'banana', 'cherry', 'date', 'fig', 'grape']
# random_values = get_random_values(my_list)
# print("Random values:", random_values)
